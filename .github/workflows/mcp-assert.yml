name: mcp-assert

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  assert:
    uses: wyre-technology/.github/.github/workflows/mcp-assert.yml@main
    with:
      entry: dist/index.js
      canary-tool: domotz_power_outlets_list
